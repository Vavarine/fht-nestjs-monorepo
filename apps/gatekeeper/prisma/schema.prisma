datasource db {
  provider = "postgresql"
}

generator client {
  provider     = "prisma-client"
  output       = "../generated/prisma"
  moduleFormat = "cjs"
}

model Customer {
  id        String   @id // Cognito sub (UUID v4)
  cpf       String   @unique
  name      String
  email     String?
  createdAt DateTime @default(now())
}
