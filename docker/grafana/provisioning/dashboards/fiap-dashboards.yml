apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: 'FIAP Hackathon'
    folderUid: fiap
    type: file
    disableDeletion: false
    editable: true
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards